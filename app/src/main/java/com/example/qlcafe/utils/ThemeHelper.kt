package com.example.qlcafe.utils

import android.view.View

object ThemeHelper {
    /**
     * Hàm áp dụng giao diện đồng nhất cho các View (Card, Item, v.v.)
     * Hiện tại hỗ trợ cơ bản, có thể mở rộng sau.
     */
    fun applyTheme(view: View) {
        // Có thể thêm logic đổi màu, bo góc, v.v. ở đây nếu cần
        // Tạm thời để trống hoặc thực hiện các thiết lập mặc định
    }
}
